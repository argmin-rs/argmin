[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
doc-comment = "0.3.3"
argmin = { path = "../../../argmin", features = ["serde1", "ctrlc", "wasm-bindgen", "rayon"] }
argmin-math = { path = "../../../argmin-math", features = ["latest_all"] }
argmin-observer-slog = { path = "../../../observers/slog", features = ["serde1"] }
argmin-observer-spectator = { path = "../../../observers/spectator" }
argmin_testfunctions = { version = "*", path = "../../../tools/testfunctions/" }
serde = "1"
